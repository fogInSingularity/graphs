# graphs
graphs homework for compiler class

## Prerequisites

- C++ compiler (with C++20 support)
- CMake (3.21+)

## Build

```bash
cmake -B build -S . -DCMAKE_BUILD_TYPE=Release
cmake --build build
```

## Usage 

```bash
./build/graph.x < graph.txt
```
